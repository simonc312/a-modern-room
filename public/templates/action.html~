<div class="action">
  <div class="display <%= enabled ? 'enabled' : 'disabled' %>">
    <span class="action-helper-right">
      <% _.each(cost, function(cost, resource){ %>
        | <%- resource %> : <%- cost %>
      <% }); %>
    </span>
    <div class="action-content" data-action-id="<%= id %>">
      <%- content %></div>
    <span class="action-destroy"></span>
  </div>
</div>
